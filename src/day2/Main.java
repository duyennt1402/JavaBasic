package day2;

public class Main {

	public static void main(String[] args) {
		/*
		 * { int x = 20; String sup = (x < 15) ? "small" : (x < 22) ? "tiny" : "huge";
		 * System.out.println(sup); }
		 */
		Operators operators = new Operators();
		// operators.compare(args);
		operators.calSpeed();

	}

}
